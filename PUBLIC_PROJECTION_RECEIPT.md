# RunnerOS Public Projection Receipt

- State: `PREVIEW` after a reviewed commit is deployed and independently verified.
- Canonical public-safe repository: `blackenedtender/runneros-public-web`.
- Private application source: `blackenedtender/runneros` (separate custody).

This repository contains a static product, support, and privacy projection only. It contains no account system, writable API, private runner logs, routes or GPS traces, local databases, backups, credentials, invite records, internal receipts, or production integration.

The Vercel configuration applies restrictive browser headers and does not rewrite private-looking paths to a public application shell. A deployment remains a preview until its exact source commit, route behavior, headers, and secret scan are independently verified.
